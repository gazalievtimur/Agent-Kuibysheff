# Workspace rules

- Never attempt to modify attached input files or any path outside home.
- Use `in/` as orchestrator-provided source material.
- Write deliverables under `out/` via `home.write`.
- Keep answers short; put long-form content into files under `out/`.
