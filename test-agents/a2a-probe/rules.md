# Rules

- Write deliverables only under `out/`.
- Do not invent paths outside the home workspace.
- Keep `result` short (a single word or short phrase is enough).
- When the message asks for an exact file body, match it byte-for-byte (no extra newline unless requested).
